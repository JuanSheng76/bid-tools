# 标策台 — 代码功能说明

> 本文档用通俗语言解释项目里每个文件是做什么的，面向非开发人员。

---

## 📋 总览

整个工具是**一个网站应用**，双击 `run.bat` 启动后，浏览器访问 `http://127.0.0.1:8000` 就能使用。

> 💡 `127.0.0.1` 是"本机地址"，只能在自己电脑上访问。想让同事也能用，需要改为你的局域网 IP（如 `192.168.x.x`），参考 CLAUDE.md 中的说明。

所有代码按职责放在四个层级里：

| 层级 | 目录/文件 | 职责（比喻） |
|------|-----------|-------------|
| 入口 | `main.py`、`run.bat` | 大门 + 电闸 |
| 路由 | `routers/` | 每个页面的"前台接待员" |
| 服务 | `services/` | 后台干活的人（算分、爬虫、倒排） |
| 模板 | `templates/` | 页面的"装修和布局" |

---

## 🚀 启动相关

### `run.bat` — 一键启动脚本
双击自动完成 4 步：检查 Python → 安装依赖 → 建数据库 → 导入标讯来源 → 启动网站。**这是唯一需要操作的文件。**

### `main.py` — 网站入口
整个网站的"总开关"。负责启动服务器、注册所有页面路由、设置定时爬虫（每30分钟自动抓一次标讯）。

### `requirements.txt` — 依赖清单
列出项目用到的 12 个第三方 Python 包（相当于盖房子需要的建材清单），`run.bat` 会自动安装。

---

## 🗄️ 数据库相关

### `database.py` — 数据库连接
负责连接 SQLite 数据库文件（`bid_tools.db`），启动时自动建表。不需要手动操作。

### `models.py` — 数据表定义
定义了 7 张数据表的结构：用户、公司资料、标讯来源、标讯信息、报名记录、任务、开标结果。可以理解为"Excel 表格的表头定义"。

### `config.py` — 系统配置
存放各种预设参数：
- **任务模板**：倒排计划的 7 个标准阶段（招标文件获取、资质准备、技术方案……）
- **评分权重**：资质 40%、业绩 25%、人员 15%、财务 10%、其他 10%
- **阈值**：≥70 分推荐投，40-69 可考虑，<40 不推荐

### `schemas.py` — 数据校验规则
定义数据格式的校验规则（前端用的不多，主要是 API 层面的类型检查）。

### `bid_tools.db` — 数据库文件
SQLite 数据库文件，所有数据都存在这里。**备份数据时只需复制这个文件。**

---

## 🧭 页面路由（`routers/`）

每个文件对应网站上一个功能模块的"后台处理"。

### `routers/dashboard.py` — 仪表盘首页
打开网站看到的第一个页面。处理：
- 顶部 4 个统计卡片（待处理标讯、进行中投标、待完成任务、累计中标）
- 近期提醒面板（7 天内截止的标讯和任务，任务会显示所属标讯）
- 近 12 个月中标趋势图
- 近期标讯列表和待办任务列表

### `routers/notices.py` — 标讯管理
标讯的增删改查 + 评估 + 决策 + Excel 导出。包括：
- 标讯列表（分页、按状态/关键词筛选）
- 标讯详情（完整信息 + 评估结果 + 操作按钮）
- 手动录入标讯
- 编辑/删除标讯
- 触发评估（自动评分）
- 投标决策（人工决定投还是不投）
- 导出标讯列表为 Excel

### `routers/tasks.py` — 任务看板 + 倒排计划
- **任务看板**：三列显示（待开始/进行中/已完成），支持拖拽切换状态
- **倒排计划生成**：两步操作 —— 先设置截止日期和各阶段天数，确认后自动生成 7 个标准任务
- **重排计划**：删除旧任务，按新参数重新生成
- 任务编辑、分配负责人、勾选 checklist

### `routers/calendar.py` — 投标日历
月视图日历，标记所有标讯的投标截止日期。颜色区分紧急程度：
- 🔴 红色：7 天内截止
- 🟡 黄色：14 天内截止
- 🔵 蓝色：时间充裕
- ⚫ 灰色：已过期

### `routers/results.py` — 开标结果 + 合同管理
管理投标结果：开标记录、中标/未中标、竞对报价、合同签订、合同到期跟踪。支持导出 Excel。

### `routers/company.py` — 公司资料
编辑公司的基本信息和三大类资源：
- **资质证书**：名称、等级、编号、有效期
- **业绩案例**：项目名称、金额、合同日期
- **人员信息**：姓名、职位、证书、联系方式
- **银行信息**：开户行、账号、税号

这些数据是评估引擎的比对基础。

### `routers/sources.py` — 标讯来源管理
配置自动爬虫的采集来源。每个来源可设置：
- 目标网站 URL
- CSS 选择器（从哪里抓标题、日期、链接）
- 爬取间隔
- 启用/禁用

支持手动触发单次爬取。

### `routers/registrations.py` — 报名管理
管理标讯的报名流程：
- 报名记录创建（关联标讯）
- 自动填表（从公司资料一键填充报名表单）
- 报名状态流转（待提交 → 已提交 → 已确认/已拒绝）
- 缴费状态跟踪（未缴费 ↔ 已缴费）

### `auth.py` — 登录/注册
处理用户登录、注册、权限管理。首个注册用户自动成为管理员，后续用户为普通员工。

---

## ⚙️ 业务逻辑（`services/`）

### `services/assessor.py` — 评估引擎
自动评估一个标讯值不值得投。从 5 个维度打分：
1. **资质匹配（40分）**：公司资质是否满足要求
2. **业绩匹配（25分）**：是否有同类项目经验
3. **人员匹配（15分）**：人员证书是否匹配
4. **财务能力（10分）**：预算是否合理
5. **其他因素（10分）**：平台注册、标书费、期限、区域

### `services/planner.py` — 倒排计划生成器
根据投标截止日期，向前倒推生成 7 个标准任务：
1. 招标文件获取
2. 资质文件准备
3. 技术方案编写
4. 商务报价编制
5. 标书合成与盖章
6. 标书密封与递交
7. 开标参与

每个任务自动跳过周末，计算工作日。

### `services/scraper.py` — 通用爬虫
配置驱动的网页采集引擎。不需要为每个网站单独写代码，通过 JSON 配置就能抓取。支持：
- 列表页采集（标题、链接、日期）
- 详情页采集（截止日期、预算、联系方式）
- 自动去重（根据 external_id）
- 分页翻页

### `services/reminders.py` — 提醒服务
查询即将到期的项目：
- 标讯投标截止（7 天内）
- 任务计划到期（7 天内）
- 合同到期（30 天内）

按紧急程度排序，显示在仪表盘首页。

---

## 🎨 页面模板（`templates/`）

所有 `.html` 文件决定了网站的外观和布局。

### `templates/base.html` — 基础框架
所有页面的"母版"，包含：
- 顶部导航栏（毛玻璃效果）
- 左侧菜单栏（深色渐变背景 + 激活指示条）
- 完整的 CSS 设计系统（配色、圆角、阴影、按钮、表格、卡片、筛选栏、表单等）
- 全局 JavaScript 库引用（HTMX、Alpine.js、SortableJS、Chart.js）

### 各模块模板

| 文件 | 对应页面 |
|------|---------|
| `templates/dashboard.html` | 仪表盘首页 |
| `templates/auth/login.html` | 登录/注册页 |
| `templates/notices/list.html` | 标讯列表 |
| `templates/notices/detail.html` | 标讯详情 |
| `templates/notices/form.html` | 新建/编辑标讯表单 |
| `templates/tasks/kanban.html` | 任务看板（三列拖拽） |
| `templates/tasks/detail.html` | 任务详情 |
| `templates/tasks/generate.html` | 倒排计划设置表单 |
| `templates/results/list.html` | 开标结果列表 |
| `templates/company/edit.html` | 公司资料编辑 |
| `templates/sources/list.html` | 标讯来源管理 |
| `templates/registrations/list.html` | 报名记录管理 |
| `templates/calendar.html` | 投标日历月视图 |

---

## 🎯 前端交互（`static/`）

### `static/app.js` — 页面交互逻辑
处理网页上的动态效果：
- 任务看板拖拽（SortableJS）
- Checklist 勾选
- Toast 提示弹出（自动消失 + Esc 关闭）
- HTMX 无刷新交互 + 页面切换过渡动画
- 仪表盘统计数字计数动画
- 键盘快捷键（Ctrl+K 搜索、Esc 关闭提示）

### `static/style.css` — 补充样式
在 `base.html` 全局样式基础上补充的样式：拖拽效果、Toast 动画、HTMX 页面过渡、滚动条美化、打印样式等。

### `templates_config.py` — 模板公共配置
技术文件，为所有页面统一注入当前时间、注册颜色过滤器等。

---

## 🛠️ 辅助脚本

### `seed_sources.py` — 初始化标讯来源
预置 2 个标讯网站配置（中国政府采购网、全国公共资源交易平台），`run.bat` 启动时自动运行。已存在的来源会自动跳过。

### `test_scrape_urls.py` — 网络连通性测试
测试预配置的标讯网站能否正常访问。在启用爬虫之前先跑这个脚本，确认网络可达。

---

## 📁 其他文件

### `CLAUDE.md` — 开发者技术文档
给开发人员看的技术说明（技术栈、数据库结构、API 路由等），非开发人员不需要关注。

### `代码功能说明.md` — 本文档
即你正在看的这份文件，用通俗语言解释每个文件做什么。

### `.gitignore`
告诉 Git 哪些文件不需要备份（如 `__pycache__`、数据库文件等）。

### `__pycache__/`
Python 自动生成的缓存文件夹，可以忽略。

---

## 🔑 关键概念速查

| 概念 | 说明 |
|------|------|
| **标讯** | 一条招标信息 |
| **评估** | 系统自动分析标讯值不值得投（打分 0-100） |
| **决策** | 人最终决定投还是不投（不管系统打多少分） |
| **倒排计划** | 从投标截止日向前推算，生成各阶段任务 |
| **任务看板** | 三列（待开始/进行中/已完成）可视化任务管理 |
| **标讯来源** | 爬虫自动抓取标讯的目标网站 |
| **报名** | 投标前在招标平台的报名登记 |
| **开标结果** | 投标后的结果记录 + 合同管理 |
