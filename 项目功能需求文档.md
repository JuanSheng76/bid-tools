# 标策台 — 功能需求文档

> 面向小团队（2-5人）的轻量化投标管理系统。目标行业：光伏检测、招标代理。

---

## 概述

这是一个面向小型投标团队的轻量化管理系统，覆盖投标工作的完整流程：从发现标讯 → 评估可行性 → 决定是否投标 → 制定投标计划 → 准备投标材料 → 跟踪开标结果 → 管理合同。

**核心思路**：让系统替团队记住所有细节，减少人工跟进和重复劳动。

1. **标讯发现与评估**：自动爬取标讯信息，系统根据公司资质自动评分，帮团队快速筛选值得投的项目
2. **投标决策与计划**：人工确认投标后，一键生成倒排任务计划，7个阶段、每个阶段有 checklist
3. **公司资料库**：统一管理公司的资质证书、项目业绩、人员信息，作为评估和投标的基础数据
4. **投标文件复用**（规划中）：历史投标文件不再尘封，导入系统后可以智能推荐、自动拼装生成新的业绩材料文档
5. **招标文件解析**（规划中）：拿到招标文件后自动提取资格要求、评分标准，推荐匹配的公司资料，注意事项自动同步到任务清单

整个流程串联起来：

```
发现标讯 → 自动评估 → 人工决策 → 生成计划 → 上传招标文件解析
    ↓                                    ↓
  爬虫导入         ←── 公司资料库 ──→  推荐业绩/人员
    ↓                                    ↓
  报名跟踪 → 投标任务执行 → 开标结果 → 合同管理
```

---

## 一、用户与权限

### 1.1 用户注册与登录
- 支持用户自行注册账号，首个注册用户自动成为管理员，后续为普通成员
- 管理员和普通成员权限相同（当前版本不做权限区分）
- 登录后 7 天内保持登录状态
- 支持注销登录

### 1.2 测试账号
- 用户名：admin，密码：admin123

---

## 二、仪表盘（首页）

### 2.1 统计概览
- 首页展示关键数据统计卡片：
  - 标讯总数
  - 待处理任务数
  - 本月投标数
  - 中标率
- 数字加载时有计数动画效果

### 2.2 月度投标趋势
- 柱状图展示近 12 个月每月投标数量

### 2.3 近期提醒
- 展示三类即将到期事项：
  - 标讯投标截止（未来 7 天内）
  - 任务到期（未来 7 天内）
  - 合同到期（未来 30 天内）
- 按紧急程度排列，越临近越靠前
- 任务提醒显示关联的标讯名称
- 可点击跳转到对应详情

---

## 三、标讯管理

### 3.1 标讯列表
- 分页浏览所有标讯，每页 20 条
- 按状态筛选：新建/已评估/值得投/不值得/已报名/投标中/已完成/已忽略
- 按投标决策筛选：已决定投/已决定不投
- 关键词搜索（搜索标题、项目范围、资格要求）
- 多种排序方式：按截止日期、按预算高低、按创建时间
- 快捷视图：
  - 重点关注（值得投 + 投标中的标讯）
  - 即将到期（7天内截止）
  - 已逾期（截止日期已过）
- 每条标讯显示：标题、预算、截止日期、状态标签、决策标签

### 3.2 手动录入标讯
- 表单字段：
  - 基本信息：标题、来源链接、发布日期、项目所在地
  - 关键时间：报名截止、投标截止、开标时间、开标地点
  - 项目要求：项目范围（文本）、资格要求（文本）
  - 费用信息：预算（万元）、标书费（元）、保证金（万元）
  - 联系信息：平台名称、需平台注册（是/否）、联系人、电话、邮箱

### 3.3 标讯详情页
- **操作按钮**（根据状态动态显示）：
  - 执行评估（仅新建状态可用）
  - 决定投标 / 决定不投（未决策时可用，点击前弹确认框）
  - 生成倒排计划（决定投标后可用，已有任务时显示"重排计划"）
  - 上传招标文件解析（决定投标后可用）
  - 删除标讯（弹确认框）
  - 决策状态标签（绿色=已决定投，红色=不投）

- **基本信息区**：来源链接、发布日期、报名截止（过期变红）、投标截止（小于7天红色+倒计时天数）、开标时间/地点、预算（大号显示）、标书费、保证金、项目地点

- **平台与联系人区**：平台注册状态、联系人、电话、邮箱

- **资格要求区**：原始文本展示

- **评估结果区**（评估后显示）：
  - 左侧色条标识推荐程度（绿色=推荐投，黄色=可考虑，红色=不推荐）
  - 综合评分（大方数显示，满分100）
  - 5项分维度得分及进度条：资质匹配、业绩匹配、人员匹配、财务能力、其他因素
  - 风险提示：列出缺失项或风险说明

- **招标文件解析区**（上传解析后显示）：
  - 资格要求摘要
  - 评分标准详情
  - 推荐匹配的公司资料
  - 重要注意事项

- **可编辑区**（可折叠）：所有字段可修改

### 3.4 标讯状态流转
```
新建 → 已评估 → 值得投 → 已报名 → 投标中 → 已完成
              → 不值得 → 已忽略
```
- 评估分数 ≥70 → 自动设为"值得投"
- 40-69分 → 保持"已评估"（待人工判断）
- <40分 → 自动设为"不值得"
- 生成倒排计划 → 自动设为"投标中"
- 录入开标结果 → 自动设为"已完成"

### 3.5 投标决策（人工判断）
- 评估是系统的自动建议，决策是人来做最终判断
- 即使评分低，也可手动决定投标（状态自动升为"值得投"）
- 决定不投 → 状态变为"已忽略"

### 3.6 Excel 导出
- 一键导出标讯列表为 Excel 文件
- 文件名包含导出日期，如：`标讯导出_20260729_1430.xlsx`
- 中文文件名在浏览器下载时正常显示

---

## 四、自动评估

### 4.1 评估触发
- 在标讯详情页点击"执行评估"按钮触发
- 如果公司资料未完善，提示先完善公司资料

### 4.2 评估维度与权重
- **资质匹配（40分）**：扫描标讯资格要求中是否提到资质、证书、许可证、认证等关键词，与公司已录入的资质证书比对
- **业绩匹配（25分）**：比较公司历史业绩的合同金额是否达到标讯预算的 30% 以上，统计满足条件的项目数量
- **人员匹配（15分）**：统计公司录入的人员数量和持证数量
- **财务能力（10分）**：检查公司银行账户、税号是否已填写
- **其他因素（10分）**：
  - 是否需要平台注册（不需要则得分）
  - 标书费是否合理（≤5000元得分）
  - 投标截止日期紧迫度（≥15天满分，≥7天部分得分）
  - 项目地点是否与公司注册地相近
  - 是否提供了联系人信息

---

## 五、倒排计划与任务管理

### 5.1 倒排计划生成
- 以投标截止日期为终点，倒推各阶段任务的截止日期
- 7 个标准阶段（可自定义每阶段提前天数）：
  1. 获取招标文件（默认截止前 15 个工作日）
  2. 资质项及承诺函（截止前 12 个工作日）
  3. 评分项证书/业绩整理（截止前 10 个工作日）
  4. 报价（截止前 7 个工作日）
  5. 编写服务方案（截止前 5 个工作日）
  6. 调整格式（截止前 3 个工作日）
  7. 盖章（截止前 1 个工作日）
- 工作日计算自动排除周六和周日
- 生成前可预览各阶段日期，确认后执行
- 支持"重排"模式：删除旧任务后重新生成

### 5.2 任务看板
- 三列看板视图：待开始 / 进行中 / 已完成
- 每列显示任务数量
- 列与列之间可拖拽卡片改变任务状态
- 空列显示空状态提示

### 5.3 任务卡片
- 顶部显示所属标讯名称（带颜色标签，同一标讯始终同色）
- 任务标题（已完成的任务标题加删除线）
- 优先级标签：紧急（红色）、高（橙色）、中（琥珀色）、低（灰色）
- 负责人（未分配时提示"未分配负责人"）
- 截止日期，逾期任务用红色标记
- 可快速切换状态

### 5.4 任务详情
- 显示任务基本信息：标题、优先级、状态、类型、关联标讯、计划起止日期
- 可为任务分配负责人
- 可编辑任务标题、描述、优先级、日期
- 每个任务有预设的 checklist（3项），可自由增删勾选：
  - 获取招标文件：查阅招标公告全文 / 下载招标文件 / 确认投标人资格要求
  - 资质项及承诺函：整理资质证书复印件 / 撰写承诺函声明函 / 法人授权委托书
  - 评分项证书业绩整理：整理评分项证书 / 整理同类项目业绩证明 / 准备人员证书社保
  - 报价：核算成本 / 确定投标报价 / 填写报价表
  - 编写服务方案：编写技术方案 / 编写服务承诺 / 整理公司简介
  - 调整格式：检查页码目录 / 统一字体格式 / 按要求装订
  - 盖章：逐页盖章 / 法定代表人签字 / 密封投标文件

### 5.5 任务筛选
- 按所属标讯筛选
- 按负责人筛选
- 快捷链接：只看我的任务 / 只看已逾期

---

## 六、开标结果与合同管理

### 6.1 开标结果录入
- 记录开标日期、参标企业数量、我方报价
- 录入竞对报价（可添加多家，每家录入公司名+报价）
- 结果选项：中标 / 未中标 / 废标 / 取消

### 6.2 合同管理
- 中标后记录合同信息：合同金额、签约日期、合同到期日期
- 合同到期出现在仪表盘提醒中（30天内）

### 6.3 Excel 导出
- 结果列表可导出为 Excel，格式同标讯导出

---

## 七、公司资料管理

### 7.1 基本信息
- 公司名称、统一社会信用代码、法定代表人、注册地址
- 默认联系人、联系电话、联系邮箱
- 银行与税务信息（可折叠，低频使用）：开户行、账号、纳税人识别号

### 7.2 资质证书
- 列表展示已录入的资质
- 每条资质包含：名称、等级（甲级/一级等）、证书编号、发证机关、发证日期、到期日期、是否永久有效
- 支持添加新资质、删除已有资质

### 7.3 业绩记录
- 列表展示已录入的项目业绩
- 每条业绩包含：项目名称、项目类型（光伏检测/招标代理/政府采购/工程建设/其他）、合同金额（万元）、业主单位、合同日期、项目描述
- 支持添加新业绩、删除已有业绩

### 7.4 人员信息
- 列表展示已录入的人员
- 每人包含：姓名、职位、持证情况（如"一级建造师,注册检测工程师"）、联系电话、邮箱
- 支持添加新人员、删除已有人员

---

## 八、标讯来源与自动爬取

### 8.1 来源配置
- 管理标讯来源网站列表
- 每个来源可配置：名称、网址、爬取规则、是否启用定时爬取
- 支持添加、编辑、删除来源

### 8.2 自动爬取
- 系统每 30 分钟自动爬取所有启用的来源
- 支持手动触发单个来源的立即爬取
- 爬取时自动去重（同一外部 ID 不重复导入）
- 自动识别日期格式（支持多种中文日期格式）
- 自动识别金额单位（万元/万/元）

### 8.3 种子数据
- 系统初始化时预置一批常用标讯来源网站
- 重复初始化不会覆盖已有数据

---

## 九、报名管理

### 9.1 报名跟踪
- 记录每个标讯的报名状态
- 报名状态流转：待报名 → 已提交 → 已确认 / 已拒绝
- 缴费状态：未缴费 ↔ 已缴费
- 记录：平台名称、平台账号、缴费金额

### 9.2 自动填表
- 从公司资料中自动提取信息预填报名表单
- 减少重复录入

---

## 十、投标日历

### 10.1 月视图日历
- 按月展示日历，可切换月份
- 标讯截止日期在日历格中标记显示
- 不同类型日期用不同颜色区分（投标截止/开标/报名截止）
- 右侧展示本月即将截止的标讯列表

---

## 十一、投标文件库（规划中）

### 11.1 Phase 1：导入过往投标文件

**背景**：团队历史积累了大量投标文件（.docx），里面的业绩、资质、人员信息是公司的宝贵资料，需要导入到系统中统一管理，并记住每条信息来源于哪个文件。

**功能**：

1. **批量导入**：
   - 将历史投标文件 .docx 放入本地文件夹
   - 运行脚本自动扫描并导入
   - 支持预览模式（只查看提取结果，不实际写入）

2. **智能提取**：
   - 自动识别投标文件中的业绩章节、资质章节、人员章节
   - 从业绩章节中提取：项目汇总表 + 每个项目的详细描述 + 证明图片
   - 从资质章节中提取：资质汇总表 + 证书扫描件
   - 从人员章节中提取：人员列表 + 每人持有的证书 + 每人做过的项目业绩

3. **去重处理**：
   - 同一个项目出现在多个投标文件中（不同年份的标书常重复使用业绩），只保留一份
   - 项目名称相似度超过 80% 视为重复
   - 重复时保留合同日期更新的版本
   - 同名人员自动合并其个人业绩，不重复添加

4. **来源追踪**：
   - 每条资质/业绩/人员记录都标注来源于哪个投标文件
   - 记录每段内容在源文件中的位置（方便后续提取引用）
   - 提取的图片保存到对应文件夹

5. **文件库浏览**：
   - 新增"投标文件库"页面，按来源文件分组展示
   - 每个文件显示提取统计：提取了 X 条资质、Y 条业绩、Z 名人员、N 张图片
   - 可展开查看具体内容
   - 公司资料页的资质/业绩/人员表格增加"来源文件"列

### 11.2 Phase 2：智能推荐并生成业绩材料文档

**背景**：制作投标文件时，需要从公司历史业绩中挑选合适的项目编入标书。系统应能根据招标要求自动推荐最匹配的业绩，用户勾选后，自动从原始投标文件中提取对应文字和图片，生成新的业绩材料 .docx，减少手动复制粘贴的工作量。

**功能**：

1. **智能推荐**：
   - 系统分析标讯的评分要求，提取关键条件（项目类型、金额规模、所需资质等）
   - 从公司资料库中匹配最符合条件的业绩、人员、资质
   - 推荐排序：匹配度最高 > 合同日期最新 > 描述最完整

2. **推荐选择页**：
   - 在标讯详情页新增"生成业绩材料"入口
   - 展示系统推荐列表，用户可以勾选/取消
   - 支持预览每条业绩在原始文件中的内容

3. **自动生成 docx**：
   - 用户确认选择后，系统自动从原始投标文件中提取对应内容
   - 包括文字描述段落和证明材料图片
   - 自动排版生成新文档，包含：
     - 封面（标讯名称、投标人、日期）
     - 资质证明部分（证书描述 + 扫描件）
     - 业绩汇总表 + 各项目详情（原始文字 + 证明图片）
     - 人员简历部分（个人信息 + 个人业绩）
   - 浏览器直接下载生成的 .docx 文件

---

## 十二、招标文件解析（规划中）

**背景**：拿到客户的招标文件后，需要逐字阅读找出资格要求、评分办法、关键注意事项。这个工作重复且容易遗漏。系统应能自动解析招标文件，提取关键信息，推荐匹配的公司资料，并把注意事项自动添加到对应的任务清单中。

**功能**：

1. **上传招标文件**：
   - 在标讯详情页（决定投标后），点击上传招标文件
   - 支持 Word (.docx) 和 PDF (.pdf) 两种格式
   - 上传后自动触发解析

2. **资格要求提取**：
   - 自动识别文档中的资格要求章节
   - 提取以下信息：
     - 密封包装要求：正本几份、副本几份、如何密封
     - 递交要求：截止时间、递交地点、递交方式
     - 盖章签字要求：哪些地方需要盖章、哪些需要签字
     - 所需资质证书：营业执照、各类资质、许可证等，并自动比对公司已有资质，标记哪些已具备、哪些缺失
     - 所需承诺函：诚信承诺函、无违法记录声明、法人证明、授权委托书等
     - 保证金要求：金额、形式（保函/转账）

3. **评分标准提取**：
   - 自动识别评分办法章节
   - 提取每个评分项的名称、满分值、评分标准
   - 归类为：报价分、技术方案分、业绩分、人员分、资质分等
   - 从评分标准中进一步提取具体条件：
     - 业绩要求：需要几个类似项目、单合同最低金额、近几年的项目、什么类型的项目
     - 人员要求：需要什么证书、多少年经验、团队至少几人

4. **智能推荐匹配**：
   - 根据提取的评分标准，自动从公司资料库中推荐：
     - 匹配的资质证书（等级满足要求）
     - 匹配的项目业绩（项目类型对得上，金额达标，时间在要求范围内）
     - 匹配的人员（持有所需证书，有相关项目经验）
   - 每条推荐显示匹配原因和匹配程度

5. **注意事项提取与同步**：
   - 从文档中自动提取一切需要特别注意的事项
   - 自动归类对应到任务类型：
     - 盖章签字类 → 同步到"盖章"任务的 checklist
     - 密封装订要求 → 同步到"调整格式"任务的 checklist
     - 保证金相关 → 同步到"报价"任务的 checklist
     - 证书材料要求 → 同步到"评分项证书/业绩整理"任务的 checklist
     - 承诺函格式要求 → 同步到"资质项及承诺函"任务的 checklist
     - 质疑期限/答疑/踏勘 → 同步到"获取招标文件"任务的 checklist
   - 同步时自动去重，不会重复添加相同的注意事项

6. **结果展示**：
   - 标讯详情页新增"招标文件分析"卡片区域
   - 包含四块内容：
     - 资格要求摘要（密封/盖章/证书/承诺函/保证金）
     - 评分标准表格（各评分项及分值，带占比柱状图）
     - 推荐匹配（Top 3 匹配的业绩/人员/资质，可点击查看完整列表）
     - 重要注意事项列表
   - 完整推荐页面支持勾选、确认、一键同步到任务清单

---

## 十三、全局 UI 特性

### 13.1 布局
- 左侧固定侧边栏导航，深色背景
- 右侧内容区，白色/浅灰背景
- 导航项：仪表盘、标讯管理、标讯来源、任务看板、投标日历、报名管理、结果管理、公司资料
- 当前页面导航项高亮

### 13.2 交互
- 页面切换平滑过渡
- 表单提交后页面无整页刷新（部分操作使用局部刷新）
- 删除操作弹出确认框
- 操作结果 toast 提示

### 13.3 空状态
- 列表无数据时显示统一空状态占位（大图标+标题+引导文案）

### 13.4 移动端适配
- 小屏幕下侧边栏收窄为图标模式
- 表格可横向滚动
